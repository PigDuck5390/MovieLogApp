import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#a97f3f",
  },

  scrollArea: {
    padding: 16,
    alignItems: "center",
  },

  poster: {
    width: "100%",
    height: 380,
    marginBottom: 20,
    borderRadius: 12,
  },
});
