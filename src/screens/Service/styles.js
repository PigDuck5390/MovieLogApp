import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },

  inner: {
    paddingTop: 40,
    paddingHorizontal: 20,
    alignItems: "center",
  },

  title: {
    fontSize: 32,
    fontWeight: "800",
    marginBottom: 20,
  },

  phone: {
    fontSize: 28,
    fontWeight: "700",
    marginBottom: 30,
    color: "#333",
  },

  desc: {
    fontSize: 18,
    lineHeight: 28,
    textAlign: "center",
    color: "#555",
  },
});
